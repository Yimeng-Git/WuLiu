<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=".dao.CarDao">

    <resultMap type="com.example.wuliu.entity.Car" id="CarMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="number" column="number" jdbcType="VARCHAR"/>
        <result property="area" column="area" jdbcType="VARCHAR"/>
        <result property="destination" column="destination" jdbcType="VARCHAR"/>
        <result property="arrivetime" column="arriveTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="CarMap">
        select
          id, number, area, destination, arriveTime
        from car
        where id = #{id}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into car(number, area, destination, arriveTime)
        values (#{number}, #{area}, #{destination}, #{arrivetime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into car(number, area, destination, arriveTime)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.number}, #{entity.area}, #{entity.destination}, #{entity.arrivetime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into car(number, area, destination, arriveTime)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.number}, #{entity.area}, #{entity.destination}, #{entity.arrivetime})
        </foreach>
        on duplicate key update
        number = values(number),
        area = values(area),
        destination = values(destination),
        arriveTime = values(arriveTime)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update car
        <set>
            <if test="number != null and number != ''">
                number = #{number},
            </if>
            <if test="area != null and area != ''">
                area = #{area},
            </if>
            <if test="destination != null and destination != ''">
                destination = #{destination},
            </if>
            <if test="arrivetime != null">
                arriveTime = #{arrivetime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from car where id = #{id}
    </delete>

    <select id="getAll" resultMap="CarMap">
            select $allSqlColumn()
            from wuliu.car
    </select>

</mapper>

