<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=".dao.ProcalamationDao">

    <resultMap type="com.example.wuliu.entity.Procalamation" id="ProcalamationMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="time" column="time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ProcalamationMap">
        select id,
               content,
               title, time
        from procalamation
        where id = #{id}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into procalamation(content, title, time)
        values (#{content}, #{title}, #{time})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into procalamation(content, title, time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.content}, #{entity.title}, #{entity.time})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into procalamation(content, title, time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.content}, #{entity.title}, #{entity.time})
        </foreach>
        on duplicate key update
        content = values(content),
        title = values(title),
        time = values(time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update procalamation
        <set>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="time != null">
                time = #{time},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from procalamation
        where id = #{id}
    </delete>

    <select id="getAll" resultMap="ProcalamationMap">
        select $allSqlColumn()
        from wuliu.procalamation
    </select>

</mapper>

