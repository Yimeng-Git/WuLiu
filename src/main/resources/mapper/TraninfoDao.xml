<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.wuliu.dao.TraninfoDao">

    <resultMap type="com.example.wuliu.entity.Traninfo" id="TraninfoMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="startTime" column="startTime" jdbcType="TIMESTAMP"/>
        <result property="tntnumber" column="tntnumber" jdbcType="VARCHAR"/>
        <result property="carnum" column="carnum" jdbcType="VARCHAR"/>
        <result property="arriveTime" column="arriveTime" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TraninfoMap">
        select id,
               startTime,
               tntnumber,
               carnum,
               arriveTime,
               status
        from traninfo
        where id = #{id}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into traninfo(startTime, tntnumber, carnum, arriveTime, status)
        values (#{startTime}, #{tntnumber}, #{carnum}, #{arriveTime}, #{status})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into traninfo(startTime, tntnumber, carnum, arriveTime, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.startTime}, #{entity.tntnumber}, #{entity.carnum}, #{entity.arriveTime}, #{entity.status})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into traninfo(startTime, tntnumber, carnum, arriveTime, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.startTime}, #{entity.tntnumber}, #{entity.carnum}, #{entity.arriveTime}, #{entity.status})
        </foreach>
        on duplicate key update
        startTime = values(startTime),
        tntnumber = values(tntnumber),
        carnum = values(carnum),
        arriveTime = values(arriveTime),
        status = values(status)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update traninfo
        <set>
            <if test="startTime != null">
                startTime = #{startTime},
            </if>
            <if test="tntnumber != null and tntnumber != ''">
                tntnumber = #{tntnumber},
            </if>
            <if test="carnum != null and carnum != ''">
                carnum = #{carnum},
            </if>
            <if test="arriveTime != null">
                arriveTime = #{arriveTime},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where tntnumber = #{tntnumber}
    </update>

    <update id="go">
        update traninfo set
        status = 1
        where tntnumber in
        <foreach collection="list" index="index" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </update>


    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from traninfo
        where tntnumber = #{tntnumber}
    </delete>

    <select id="getWay" resultMap="TraninfoMap">
        select tntnumber
        from traninfo
        <where>
            <if test="carnum != null and carnum != ''">
                and carnum = #{carnum}
            </if>
        </where>
    </select>

    <select id="getAll" resultMap="TraninfoMap">
        select tntnumber
        from traninfo
        <where>
            <if test="carnum != null and carnum != ''">
                and carnum = #{carnum}
            </if>
        </where>
    </select>

    <select id="queryAll" resultMap="TraninfoMap">
        select id,
        startTime,
        tntnumber,
        carnum,
        arriveTime,
        status
        from traninfo
        <where>

            <if test="tntnumber!=null and tntnumber!=''">
                and tntnumber like '%${tntnumber}%'
            </if>
            <if test="status!=null and status!=''">
                and status like #{status}
            </if>

        </where>
    </select>
</mapper>

