<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.wuliu.dao.UserDao">

    <resultMap type="com.example.wuliu.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="gender" column="gender" jdbcType="VARCHAR"/>
        <result property="age" column="age" jdbcType="INTEGER"/>
        <result property="address" column="address" jdbcType="VARCHAR"/>
        <result property="phonemun" column="phonemun" jdbcType="VARCHAR"/>
        <result property="eMail" column="eMail" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select id,
               username,
               password,
               gender,
               age,
               address,
               phonemun,
               eMail
        from user
        where id = #{id}
    </select>

    <!-- 登录 -->
    <select id="selectOne" resultMap="UserMap">
        select id,
               username,
               password,
               gender,
               age,
               address,
               phonemun,
               eMail
        from user
        where username = #{username}
          and password = #{password}
    </select>
    <!-- 注册 -->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into user(username, password, gender, age, address, phonemun, eMail)
        values (#{username}, #{password}, #{gender}, #{age}, #{address}, #{phonemun}, #{eMail})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user(username, password, gender, age, address, phonemun, eMail)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.gender}, #{entity.age}, #{entity.address},
            #{entity.phonemun}, #{entity.eMail})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user(username, password, gender, age, address, phonemun, eMail)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.username}, #{entity.password}, #{entity.gender}, #{entity.age}, #{entity.address},
            #{entity.phonemun}, #{entity.eMail})
        </foreach>
        on duplicate key update
        username = values(username),
        password = values(password),
        gender = values(gender),
        age = values(age),
        address = values(address),
        phonemun = values(phonemun),
        e-mail = values(e-mail)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user
        <set>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="gender != null and gender != ''">
                gender = #{gender},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="address != null and address != ''">
                address = #{address},
            </if>
            <if test="phonemun != null and phonemun != ''">
                phonemun = #{phonemun},
            </if>
            <if test="eMail != null and eMail != ''">
                eMail = #{eMail},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from user
        where id = #{id}
    </delete>

    <select id="getAll" resultMap="UserMap">
        select $allSqlColumn()
        from wuliu.user
    </select>

</mapper>

